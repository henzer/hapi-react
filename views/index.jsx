import React from 'react';

const Index = ({ title, message }) => (
  <html>
    <head>
      <title>{title}</title>
    </head>
    <body>
      <h1>{message}</h1>
    </body>
  </html>
);

export default Index;